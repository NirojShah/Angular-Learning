import { CustomNameArrArgsPipePipe } from './custom-name-arr-args-pipe-pipe';

describe('CustomNameArrArgsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomNameArrArgsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
