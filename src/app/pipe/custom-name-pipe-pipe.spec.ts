import { CustomNamePipePipe } from './custom-name-pipe-pipe';

describe('CustomNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
